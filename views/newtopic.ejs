<%- include('header')%>
<%- include('menu')%>
<div class="container">
        <div class="container index">
                <h2 class="text-center">Új Téma létrehozása </h2>
                <div class="container ">
                    <div class="row justify-content-center">
                        <div class="control-group col-8" id="fields">
                            <label class="control-label" for="field1"></label>
                            <div class="controls">
                                <%- include('msg')%> 
                                <form action="/forum/<%=id %>/<%=forumid %>/new" method="POST" role="form" autocomplete="off">
                                    <input class="form-control mytext" type="text" name="title" placeholder="A téma neve..." >
                                    <input class="form-control" type="hidden" name="author" value="<%= user.username %>">
                                    <input class="form-control" type="hidden" name="img" value="<%= user.img %>">
                                    <textarea cols="100" rows="10" class="form-control mytext" type="text" name="desc" placeholder="leírás..."></textarea>
                                    <button class="btn btn-success btn-block mb-3">Küldés</button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
                <a class="btn btn-success btn-sm mb-3" href="/forum/<%= id %>/<%= forumid %>">Vissza</a>
           </div>
    </div>  
<%- include('footer')%>